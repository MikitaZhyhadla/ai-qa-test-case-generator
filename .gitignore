# Secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key

# Local Claude Code settings (personal, not shared)
.claude/settings.local.json

# Node
node_modules/
npm-debug.log*

# OS / editor noise
.DS_Store
Thumbs.db
*.log
.vscode/

# Personal Claude Code instructions (not shared)
CLAUDE.local.md

# Workflow runtime files (lock and temporary files of the state script)
runs/*/.state.lock/
runs/*/*.tmp-*
