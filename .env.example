# Copy the variable names below into your OS environment (do NOT commit real values).
# Claude Code does not load .env files automatically; the variable must exist in the shell that starts `claude`.

# Fine-grained GitHub personal access token.
# Repository access: only the repository that stores the PBI issues.
# Permissions: Issues (Read and write), Metadata (Read).
GITHUB_PERSONAL_ACCESS_TOKEN=
